---
- name: Install crawler apt package dependencies.
  become: yes
  apt:
    name: "{{ item }}"
    state: latest
  with_items: "{{ crawler_apt_packages }}"

- name: Install Tor build dependencies.
  become: yes
  apt: 
    name: tor
    state: build-dep
