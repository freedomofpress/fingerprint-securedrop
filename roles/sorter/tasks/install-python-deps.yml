---
- name: Install sorter.py pip dependencies to python3.5 local distribution package directory.
  become: yes
  command: pip3 install -t /usr/local/lib/python3.5/dist-packages -r sorter-requirements.txt
  args:
    chdir: "~{{ ssh_username }}/FingerprintSecureDrop/fpsd/requirements"
